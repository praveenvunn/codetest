// src/setupTests.js

require("@testing-library/jest-dom/extend-expect");
// Import any other testing utilities or libraries that you want to configure globally

// You can add additional setup code here
beforeAll(() => {
  // Code to run once before all tests
});

afterAll(() => {
  // Code to run once after all tests
});

// You can add more setup or configuration as needed
